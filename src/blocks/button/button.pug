
mixin button(text, type='fill', extend=false)
    if extend
        button(class=`btn btn_type-${type} btn_extend`)&attributes(attributes)
            span.btn__text= text
    else
        button(class=`btn btn_type-${type}`)&attributes(attributes)
            span.btn__text= text