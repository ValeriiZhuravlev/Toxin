
mixin button(text, type='fill', extend=false)
    if extend
        button(class=`btn btn_type-${type} btn_extend`)
            span= text
    else
        button(class=`btn btn_type-${type}`)
            span= text